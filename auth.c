/*
 * auth.c
 * 
 * Handles all user authentication for the application.
 * Contains functions for registering new users, logging in,
 * and checking if a user already exists in users.dat
 */


#include <stdlib.h>
#include <stdio.h>
#include <string.h>


int   register_user(char *username, char *password){
    // Register a new user by adding their username and encrypted password to users.dat
    
    return 0;
}


int   login_user(char *username, char *password){
    // Log in a user by checking their credentials against the entries in users.dat

    return 0;
}


int   user_exists(char *username){
    // Check if a user with the given username already exists in users.dat

    return 0;
}
